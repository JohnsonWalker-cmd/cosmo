import { PagePlaceholder } from "@/components/PagePlaceholder"

export function CartPage() {
  return (
    <PagePlaceholder
      title="Cart"
      body="A slide-in cart on mobile, quantity controls, and a running total."
    />
  )
}

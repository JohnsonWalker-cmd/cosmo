import { PagePlaceholder } from "@/components/PagePlaceholder"

export function CheckoutPage() {
  return (
    <PagePlaceholder
      title="Checkout"
      body="Guest checkout: name, phone, order summary, pickup details, then Paystack."
    />
  )
}

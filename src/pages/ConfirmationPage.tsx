import { PagePlaceholder } from "@/components/PagePlaceholder"

export function ConfirmationPage() {
  return (
    <PagePlaceholder
      title="Order confirmed"
      body="After payment, customers will see their order reference and pickup instructions."
    />
  )
}
